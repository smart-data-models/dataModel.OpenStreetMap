---
# Copyleft (c) 2022 Contributors to Smart Data Models initiative
# 


components:
  schemas: 
    OSMTelecom: 
      $ref: "https://smart-data-models.github.io/dataModel.OpenStreetMap/OSMTelecom/model.yaml#/OSMTelecom"
info:
  description:  |
    Telecommunication infrastructure from OpenStreetMap tagged with telecom=*. This data model is a derivative work based on the OpenStreetMap Wiki, licensed under CC BY-SA 2.0 by OpenStreetMap contributors.
  title: OSMTelecom
  version: "0.0.1"
openapi: "3.0.0"

paths:
  /ngsi-ld/v1/entities:
    get:
      description: "Retrieve a set of entities which matches a specific query from an NGSI-LD system"
      parameters:
        -
          in: query
          name: type
          required: true
          schema:
            enum:
              - OSMTelecom
            type: string
      responses:
        ? "200"
        :
          content:
            application/ld+json:
              examples:
                keyvalues:
                  summary: "Key-Values Pairs"
                  value:
                    -
                      $ref: "https://smart-data-models.github.io/dataModel.OpenStreetMap/OSMTelecom/examples/example.json"
                normalized:
                  summary: "Normalized NGSI-LD"
                  value:
                    -
                      $ref: "https://smart-data-models.github.io/dataModel.OpenStreetMap/OSMTelecom/examples/example-normalized.jsonld"
          description: OK
      tags:
        - ngsi-ld
tags:
  -
    description: "NGSI-LD Linked-data Format"
    name: ngsi-ld
